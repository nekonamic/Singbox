<Page x:Class="Singbox.Views.Pages.NewProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Singbox.Views.Pages"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="NewProfilePage"
      d:DataContext="{d:DesignInstance local:NewProfilePage,
                                        IsDesignTimeCreatable=False}"
      d:DesignHeight="450"
      d:DesignWidth="800"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      mc:Ignorable="d">

    <StackPanel>
        <TextBlock Text="Name" HorizontalAlignment="Left" FontWeight="Bold" FontSize="16" Margin="0,0,0,8"  />
        <TextBox Margin="0,0,0,16" />
        <TextBlock Text="Type" HorizontalAlignment="Left" FontWeight="Bold" FontSize="16" Margin="0,0,0,8"  />
        <ComboBox
                Margin="0,0,0,16"
                SelectedIndex="{Binding TypeSelectionIndex, Mode=TwoWay}">
            <ComboBoxItem Content="Local" />
            <ComboBoxItem Content="Remote" />
        </ComboBox>
        <StackPanel Visibility="{Binding IsLocalVisible}">
            <TextBlock Text="File" HorizontalAlignment="Left" FontWeight="Bold" FontSize="16" Margin="0,0,0,8"  />
            <ComboBox
                    Margin="0,0,0,16"
                    SelectedIndex="0">
                <ComboBoxItem Content="Create Now" />
                <ComboBoxItem Content="Import" />
            </ComboBox>
        </StackPanel>
        <StackPanel Visibility="{Binding IsRemoteVisible}">
            <TextBlock Text="URL" HorizontalAlignment="Left" FontWeight="Bold" FontSize="16" Margin="0,0,0,8"  />
            <TextBox Margin="0,0,0,16" />
            <TextBlock Text="Auto Update" HorizontalAlignment="Left" FontWeight="Bold" FontSize="16" Margin="0,0,0,8"  />
            <ComboBox
                SelectedIndex="0">
                <ComboBoxItem Content="Enabled" />
                <ComboBoxItem Content="Disabled" />
            </ComboBox>
            <TextBlock Text="Auto Update Interval (Minutes)" HorizontalAlignment="Left" FontWeight="Bold" FontSize="16" Margin="0,0,0,8"  />
            <TextBox Margin="0,0,0,16" />
        </StackPanel>
        <Button Command="{Binding ShowDialogCommand}" Content="Create" />
    </StackPanel>
</Page>
